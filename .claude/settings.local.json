{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"c:/Users/User/biblioteca-escolar\" && git add -A && git diff --cached --quiet || (git commit -m \"auto: salvar alterações\" && git push origin main) 2>/dev/null || true",
            "statusMessage": "Enviando para o GitHub..."
          }
        ]
      }
    ]
  }
}
